---
phase: 03-stt-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/ergos/stt/__init__.py
  - src/ergos/stt/types.py
  - src/ergos/stt/transcriber.py
autonomous: true

must_haves:
  truths:
    - "faster-whisper is installed and importable"
    - "WhisperTranscriber can load a model"
    - "WhisperTranscriber can transcribe audio bytes to text"
  artifacts:
    - path: "src/ergos/stt/types.py"
      provides: "Transcription result types"
      contains: "class TranscriptionResult"
    - path: "src/ergos/stt/transcriber.py"
      provides: "Whisper model wrapper"
      contains: "class WhisperTranscriber"
      exports: ["WhisperTranscriber"]
  key_links:
    - from: "transcriber.py"
      to: "faster_whisper"
      via: "WhisperModel import"
      pattern: "from faster_whisper import"
    - from: "transcriber.py"
      to: "types.py"
      via: "TranscriptionResult return"
      pattern: "TranscriptionResult"
---

<objective>
Create STT foundation with faster-whisper transcriber wrapper.

Purpose: Establish the speech-to-text types and model interface that will be used by the STT processor.
Output: STT package with TranscriptionResult types and WhisperTranscriber class.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-infrastructure/02-01-SUMMARY.md

@src/ergos/audio/types.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add faster-whisper dependency and create STT types</name>
  <files>pyproject.toml, src/ergos/stt/__init__.py, src/ergos/stt/types.py</files>
  <action>
    1. Add faster-whisper to pyproject.toml dependencies:
       - Add "faster-whisper>=1.0" to dependencies list

    2. Create src/ergos/stt/ package directory

    3. Create src/ergos/stt/types.py with:
       - TranscriptionSegment dataclass: text, start, end, confidence (word-level result)
       - TranscriptionResult dataclass: text (full), segments list, language, duration_ms
       - TranscriptionCallback type alias: Callable[[TranscriptionResult], Awaitable[None]]
       - Use dataclasses like audio/types.py pattern

    4. Create src/ergos/stt/__init__.py exporting types
  </action>
  <verify>python -c "from ergos.stt.types import TranscriptionResult, TranscriptionSegment"</verify>
  <done>STT types importable, faster-whisper in dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create WhisperTranscriber wrapper</name>
  <files>src/ergos/stt/transcriber.py, src/ergos/stt/__init__.py</files>
  <action>
    1. Create src/ergos/stt/transcriber.py with WhisperTranscriber class:

       - __init__(self, model_size: str = "base.en", device: str = "auto", compute_type: str = "auto"):
         - Store config but don't load model yet (lazy load)
         - device="auto" means faster-whisper picks best (CUDA if available)
         - compute_type="auto" means faster-whisper picks optimal (float16/int8)

       - _ensure_model(self) -> WhisperModel:
         - Lazy load model on first use
         - Log model loading (this can take time)
         - Cache in self._model

       - transcribe(self, audio_data: bytes, sample_rate: int = 16000) -> TranscriptionResult:
         - Convert bytes to numpy array (np.frombuffer, dtype=np.int16)
         - Normalize to float32 [-1, 1] range (divide by 32768.0)
         - Call model.transcribe() with beam_size=5, language="en"
         - Build TranscriptionResult from segments
         - Include word timestamps if available (word_timestamps=True)

       - transcribe_stream(self, audio_data: bytes, sample_rate: int = 16000) -> Iterator[TranscriptionSegment]:
         - Same as transcribe but yields segments as they're generated
         - For streaming partial results to caller

       - @property model_loaded(self) -> bool: return self._model is not None

       - close(self): release model resources

    2. Update src/ergos/stt/__init__.py to export WhisperTranscriber

    IMPORTANT: Use numpy for audio conversion (already have torch which includes numpy).
    Do NOT install soundfile/librosa - faster-whisper handles raw numpy arrays directly.
  </action>
  <verify>python -c "from ergos.stt import WhisperTranscriber; t = WhisperTranscriber('tiny.en'); print('Transcriber created')"</verify>
  <done>WhisperTranscriber class created and importable, lazy model loading works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -e .` succeeds with faster-whisper
- [ ] `python -c "from ergos.stt import WhisperTranscriber, TranscriptionResult"` works
- [ ] No TypeScript/Python errors in stt module
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- faster-whisper dependency added to pyproject.toml
- STT types and transcriber exportable from ergos.stt
- WhisperTranscriber can be instantiated (model loading is lazy)
</success_criteria>

<output>
After completion, create `.planning/phases/03-stt-pipeline/03-01-SUMMARY.md`
</output>
