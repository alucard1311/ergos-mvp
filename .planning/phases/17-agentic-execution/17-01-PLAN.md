---
phase: 17-agentic-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ergos/tools/__init__.py
  - src/ergos/tools/registry.py
  - src/ergos/tools/executor.py
  - src/ergos/tools/builtins.py
  - tests/unit/test_tool_registry.py
  - tests/unit/test_tool_executor.py
autonomous: true
requirements: [AGENT-01, AGENT-04]

must_haves:
  truths:
    - "YAML tool definitions load into ChatCompletionTool-compatible dicts"
    - "Tool executor dispatches by name and returns result strings"
    - "file_read tool reads a file and returns its contents"
    - "shell_run tool executes a command with timeout and returns stdout"
    - "shell_run enforces allowed_prefixes from YAML — commands not matching any prefix are rejected"
    - "file_list tool lists directory contents with optional glob"
    - "Adding a new tool entry to YAML makes it available after reload()"
  artifacts:
    - path: "src/ergos/tools/__init__.py"
      provides: "Module exports: ToolRegistry, ToolExecutor"
      exports: ["ToolRegistry", "ToolExecutor"]
    - path: "src/ergos/tools/registry.py"
      provides: "YAML registry loader -> ChatCompletionTool list"
      min_lines: 40
    - path: "src/ergos/tools/executor.py"
      provides: "Dispatcher: name -> implementation -> result str"
      min_lines: 30
    - path: "src/ergos/tools/builtins.py"
      provides: "Built-in tool implementations: file_read, shell_run (with allowlist), file_list"
      min_lines: 60
    - path: "tests/unit/test_tool_registry.py"
      provides: "Tests for YAML loading, validation, reload"
      min_lines: 40
    - path: "tests/unit/test_tool_executor.py"
      provides: "Tests for tool dispatch, file_read, shell_run (including allowlist), file_list"
      min_lines: 70
  key_links:
    - from: "src/ergos/tools/registry.py"
      to: "YAML files in tools_dir"
      via: "yaml.safe_load + glob"
      pattern: "yaml\\.safe_load.*glob"
    - from: "src/ergos/tools/executor.py"
      to: "src/ergos/tools/builtins.py"
      via: "impl string dispatch"
      pattern: "builtin\\."
    - from: "src/ergos/tools/builtins.py"
      to: "allowed_prefixes config"
      via: "command prefix validation before execution"
      pattern: "allowed_prefixes"
---

<objective>
Create the tool infrastructure layer: YAML registry loader, tool executor dispatcher, and built-in tool implementations (file_read, shell_run with command allowlist, file_list).

Purpose: Provides the foundation for agentic execution. Tools are defined in YAML (AGENT-04), loaded into LLM-compatible schemas (AGENT-01), and dispatched to implementations that return result strings. shell_run includes a configurable command allowlist per RESEARCH.md Pitfall 5 for safety.
Output: `src/ergos/tools/` module with registry, executor, builtins, and full test coverage.
</objective>

<execution_context>
@/home/vinay/.claude/get-shit-done/workflows/execute-plan.md
@/home/vinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-agentic-execution/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/ergos/llm/types.py:
```python
@dataclass
class GenerationConfig:
    max_tokens: int = 512
    temperature: float = 0.7
    top_p: float = 0.9
    stop_sequences: list[str] = field(default_factory=list)

@dataclass
class CompletionResult:
    text: str
    tokens_generated: int
    prompt_tokens: int
    finish_reason: str = "stop"
```

From src/ergos/config.py:
```python
class Config(BaseModel):
    server: ServerConfig
    stt: STTConfig
    llm: LLMConfig
    tts: TTSConfig
    persona: PersonaConfig
```

YAML tool registry format (from RESEARCH.md, extended with allowed_prefixes for shell_run):
```yaml
tools:
  - name: file_read
    description: "Read the contents of a file at the given path"
    impl: builtin.file_read
    parameters:
      type: object
      properties:
        path:
          type: string
          description: "Absolute or home-relative file path"
      required: [path]

  - name: shell_run
    description: "Run a shell command and return stdout. Timeout is 10 seconds."
    impl: builtin.shell_run
    allowed_prefixes:
      - "ls"
      - "cat"
      - "head"
      - "tail"
      - "wc"
      - "find"
      - "grep"
      - "echo"
      - "pwd"
      - "whoami"
      - "date"
      - "df"
      - "du"
      - "uname"
      - "python3"
      - "uv run"
    parameters:
      type: object
      properties:
        command:
          type: string
          description: "Shell command to execute"
        timeout_seconds:
          type: integer
          description: "Max seconds to wait (default 10, max 30)"
      required: [command]
```

ChatCompletionTool format (passed to LLM, `_impl` and `_config` stripped):
```python
{
    "type": "function",
    "function": {
        "name": "file_read",
        "description": "Read the contents of a file",
        "parameters": { "type": "object", "properties": {...}, "required": [...] },
    },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tool registry with YAML loading and validation</name>
  <files>src/ergos/tools/__init__.py, src/ergos/tools/registry.py, tests/unit/test_tool_registry.py</files>
  <behavior>
    - Test: load_tool_registry with valid YAML produces correct ChatCompletionTool dicts (name, description, parameters present; _impl and _config stripped from LLM-facing output)
    - Test: load_tool_registry with empty directory returns empty list
    - Test: load_tool_registry with missing directory returns empty list (no error)
    - Test: Multiple YAML files are merged and sorted
    - Test: reload() picks up a newly added tool entry
    - Test: get_tools() returns dicts WITHOUT _impl or _config keys (clean for LLM)
    - Test: get_impl_map() returns {name: impl_string} mapping for executor dispatch
    - Test: get_tool_config() returns per-tool config dict (e.g., allowed_prefixes for shell_run)
    - Test: Invalid YAML entries (missing name or description) are skipped with warning
  </behavior>
  <action>
    Create `src/ergos/tools/__init__.py` exporting ToolRegistry, ToolExecutor.

    Create `src/ergos/tools/registry.py` with class `ToolRegistry`:
    - `__init__(self, tools_dir: str = "~/.ergos/tools")` — stores expanded path
    - `load(self) -> None` — scans `tools_dir/*.yaml`, parses with `yaml.safe_load`, validates each entry has `name`, `description`, `parameters`, `impl`. Stores internally as list of dicts with both LLM-facing fields, `_impl`, and `_config` (a dict of extra fields like `allowed_prefixes`).
    - `reload(self) -> None` — alias for `load()` (satisfies AGENT-04 hot-reload path)
    - `get_tools(self) -> list[dict]` — returns ChatCompletionTool-compatible dicts with `_impl` and `_config` stripped (these go to `create_chat_completion(tools=...)`)
    - `get_impl_map(self) -> dict[str, str]` — returns `{tool_name: impl_string}` for executor dispatch
    - `get_tool_config(self, tool_name: str) -> dict` — returns per-tool config dict (extra fields beyond name/description/parameters/impl). For shell_run this includes `allowed_prefixes`. Returns empty dict if tool not found.
    - `has_tools` property — True if any tools loaded

    When loading YAML entries, known schema fields are: `name`, `description`, `parameters`, `impl`. Any OTHER top-level fields (like `allowed_prefixes`) are stored in `_config[field_name]` for the tool. This makes the registry generic — any tool can carry extra config without code changes.

    Validation: skip entries missing required fields, log warning. Use `logging.getLogger(__name__)`.

    Write tests FIRST in `tests/unit/test_tool_registry.py` using `tmp_path` fixture for YAML file creation. Mock nothing — use real YAML files in temp dirs.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tool_registry.py -x -q</automated>
  </verify>
  <done>ToolRegistry loads YAML, produces clean ChatCompletionTool dicts, supports reload, stores per-tool config (e.g., allowed_prefixes), handles edge cases. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tool executor and built-in implementations with shell_run allowlist</name>
  <files>src/ergos/tools/executor.py, src/ergos/tools/builtins.py, tests/unit/test_tool_executor.py</files>
  <behavior>
    - Test: executor.execute("file_read", {"path": "/tmp/test.txt"}) returns file contents
    - Test: file_read with nonexistent path returns "Error: ..." string (no exception)
    - Test: file_read with home-relative path (~/) expands correctly
    - Test: file_read truncates output at 4096 chars
    - Test: executor.execute("shell_run", {"command": "echo hello"}) returns "hello" (echo is in default allowlist)
    - Test: shell_run with command "rm -rf /tmp/foo" returns "Error: command not allowed" (rm not in allowlist)
    - Test: shell_run with command "sudo reboot" returns "Error: command not allowed" (sudo not in allowlist)
    - Test: shell_run with empty allowed_prefixes list rejects all commands
    - Test: shell_run with no allowed_prefixes config (None) allows all commands (backwards-compatible)
    - Test: shell_run with timeout_seconds=1 on a slow command returns timeout error
    - Test: shell_run truncates output at 4096 chars
    - Test: executor.execute("file_list", {"directory": "/tmp"}) returns file listing
    - Test: file_list with pattern="*.txt" filters results
    - Test: file_list with nonexistent directory returns error string
    - Test: executor.execute("unknown_tool", {}) returns "Error: unknown tool" string
  </behavior>
  <action>
    Create `src/ergos/tools/builtins.py` with async functions:
    - `async def file_read(path: str) -> str` — expand `~`, read file, truncate to 4096 chars. Return error string on FileNotFoundError/PermissionError.
    - `async def shell_run(command: str, timeout_seconds: int = 10, allowed_prefixes: list[str] | None = None) -> str` — **Before execution, validate command against allowed_prefixes** (per RESEARCH.md Pitfall 5):
      - If `allowed_prefixes` is not None (list provided, even if empty): strip leading whitespace from command, check if `command.lstrip()` starts with any prefix in the allowlist. If no match, return `"Error: command not allowed. Permitted prefixes: {', '.join(allowed_prefixes)}"`. Log the rejected command at WARNING level.
      - If `allowed_prefixes` is None (field not in YAML config at all): allow any command (backwards-compatible for user-defined tools).
      - After validation passes: use `asyncio.create_subprocess_exec("/bin/sh", "-c", command, ...)` with `asyncio.wait_for` timeout. Cap timeout_seconds at 30. Truncate output at 4096 chars. Return error string on timeout or exception.
      - Log every shell command invocation at INFO level (command + result summary).
    - `async def file_list(directory: str, pattern: str = "*") -> str` — expand `~`, use `pathlib.Path.glob(pattern)`, return newline-joined file names. Return error string if dir doesn't exist.

    Create `src/ergos/tools/executor.py` with class `ToolExecutor`:
    - `__init__(self, impl_map: dict[str, str], registry: ToolRegistry)` — stores impl_map and registry reference (needed to look up per-tool config)
    - `async def execute(self, tool_name: str, arguments: dict) -> str` — look up `impl_map[tool_name]`, parse `builtin.{func_name}` pattern, call the matching function from `builtins.py`. For `shell_run`, inject `allowed_prefixes` from `registry.get_tool_config("shell_run").get("allowed_prefixes")` into the function call. Return error string for unknown tools.
    - Built-in dispatch table: `{"builtin.file_read": builtins.file_read, "builtin.shell_run": builtins.shell_run, "builtin.file_list": builtins.file_list}`

    Update `src/ergos/tools/__init__.py` to export ToolExecutor.

    Write tests FIRST in `tests/unit/test_tool_executor.py`. Use `tmp_path` for file operations. Use real `asyncio` for shell tests (mark with `@pytest.mark.asyncio`). For allowlist tests, create a mock registry that returns controlled configs.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tool_executor.py -x -q</automated>
  </verify>
  <done>ToolExecutor dispatches tool calls to built-in implementations. file_read, shell_run (with allowlist enforcement), file_list all work with error handling and truncation. Dangerous commands (rm, sudo, etc.) are rejected by default allowlist. All tests pass.</done>
</task>

</tasks>

<verification>
All tool infrastructure tests pass:
```bash
uv run pytest tests/unit/test_tool_registry.py tests/unit/test_tool_executor.py -x -q
```
</verification>

<success_criteria>
- ToolRegistry loads YAML files and produces ChatCompletionTool-compatible dicts
- ToolRegistry stores per-tool config (allowed_prefixes) separately from LLM-facing schema
- ToolExecutor dispatches tool calls by name to built-in implementations
- shell_run enforces allowed_prefixes — commands not matching are rejected with error message
- Default YAML ships with conservative allowlist (ls, cat, head, tail, wc, find, grep, echo, pwd, etc.)
- Built-in tools (file_read, shell_run, file_list) work with proper error handling
- Adding a tool to YAML and calling reload() makes it available
- All edge cases handled (missing files, timeouts, unknown tools, disallowed commands)
</success_criteria>

<output>
After completion, create `.planning/phases/17-agentic-execution/17-01-SUMMARY.md`
</output>
