---
phase: 08-webrtc-transport
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/ergos/transport/signaling.py
  - src/ergos/transport/__init__.py
autonomous: true

must_haves:
  truths:
    - "Outbound audio track added to connection before answer"
    - "Incoming audio track events handled"
    - "Data channel events routed to handler"
    - "Audio flows bidirectionally over WebRTC"
  artifacts:
    - path: "src/ergos/transport/signaling.py"
      provides: "Complete WebRTC signaling with tracks"
      contains: "pc.addTrack"
      min_lines: 80
  key_links:
    - from: "signaling.py"
      to: "TTSAudioTrack"
      via: "addTrack before answer"
      pattern: "pc\\.addTrack\\(.*track"
    - from: "signaling.py"
      to: "DataChannelHandler"
      via: "register_channel"
      pattern: "handler\\.register_channel"
---

<objective>
Wire audio tracks and data channels into signaling flow.

Purpose: Complete the WebRTC signaling to include audio track setup and data channel handling.
Output: Full signaling flow that establishes bidirectional audio and data channel communication.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-webrtc-transport/08-RESEARCH.md
@.planning/phases/08-webrtc-transport/08-02-SUMMARY.md
@.planning/phases/08-webrtc-transport/08-03-SUMMARY.md

@src/ergos/transport/audio_track.py
@src/ergos/transport/connection.py
@src/ergos/transport/data_channel.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance signaling with track and channel setup</name>
  <files>src/ergos/transport/signaling.py</files>
  <action>
Enhance create_signaling_app to accept handlers and wire tracks/channels.

1. Update create_signaling_app signature:
   create_signaling_app(
       manager: ConnectionManager,
       data_handler: DataChannelHandler,
       on_incoming_audio: Optional[Callable] = None  # callback for incoming audio frames
   ) -> web.Application

2. In offer() handler, after creating connection:

   a. Create TTSAudioTrack for outbound audio:
      tts_track = TTSAudioTrack(sample_rate=24000)
      pc.addTrack(tts_track)
      # Store reference for later: store on connection or in dict

   b. Register track event handler (@pc.on("track")):
      - If track.kind == "audio":
        - Create asyncio task to process incoming audio
        - Call on_incoming_audio callback if provided

   c. Register datachannel event handler (@pc.on("datachannel")):
      - Call data_handler.register_channel(channel)
      - Track channel in manager

3. Create async helper for incoming audio:
   async def process_incoming_audio(track, callback):
       while True:
           try:
               frame = await track.recv()
               if callback:
                   # Convert AudioFrame to numpy, call callback
                   samples = frame.to_ndarray()
                   await callback(samples, frame.sample_rate)
           except MediaStreamError:
               break

4. CRITICAL: Add track BEFORE createAnswer() per RESEARCH.md pitfall #6

5. Return both the app AND provide a way to get the tts_track for a connection
   - Option: Store tracks in a dict keyed by connection id
   - Or: Add get_tts_track(pc) method to module

Import TTSAudioTrack from .audio_track
Import DataChannelHandler from .data_channel
Import MediaStreamError from aiortc
  </action>
  <verify>python -c "from ergos.transport.signaling import create_signaling_app; from ergos.transport import ConnectionManager, DataChannelHandler; from ergos.audio.vad import VADProcessor; from ergos.state import ConversationStateMachine; app = create_signaling_app(ConnectionManager(), DataChannelHandler(VADProcessor(), ConversationStateMachine())); print('Full signaling app created')"</verify>
  <done>Signaling includes track and channel setup</done>
</task>

<task type="auto">
  <name>Task 2: Add track registry to connection manager</name>
  <files>src/ergos/transport/connection.py</files>
  <action>
Add ability to track TTSAudioTrack instances per connection.

1. Add _tracks dict to ConnectionManager:
   _tracks: dict[RTCPeerConnection, TTSAudioTrack] = {}

2. def register_track(self, pc: RTCPeerConnection, track: TTSAudioTrack) -> None:
   - Store track reference keyed by connection

3. def get_track(self, pc: RTCPeerConnection) -> Optional[TTSAudioTrack]:
   - Return track for connection, or None

4. Update close_all() to clear _tracks dict

5. Update cleanup when connection closes to remove track from dict
  </action>
  <verify>python -c "from ergos.transport.connection import ConnectionManager; m = ConnectionManager(); print('ConnectionManager with track registry')"</verify>
  <done>ConnectionManager tracks TTSAudioTrack per connection</done>
</task>

<task type="auto">
  <name>Task 3: Final transport exports</name>
  <files>src/ergos/transport/__init__.py</files>
  <action>
Ensure all public API is exported:

Final __all__ list should include:
- TTSAudioTrack, AUDIO_PTIME
- DataChannelMessage, VADMessage, StateMessage, SignalingRequest, SignalingResponse
- ConnectionManager
- DataChannelHandler
- create_signaling_app

Verify clean public API.
  </action>
  <verify>python -c "from ergos.transport import TTSAudioTrack, ConnectionManager, DataChannelHandler, create_signaling_app, DataChannelMessage; print('Complete transport API available')"</verify>
  <done>Complete transport package API exported</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] create_signaling_app accepts data_handler parameter
- [ ] TTSAudioTrack added before createAnswer
- [ ] Incoming track handler registered
- [ ] Data channel routed to handler
- [ ] All exports work
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Phase 8 requirements satisfied:
  - TRANSPORT-01: WebRTC signaling endpoint working
  - TRANSPORT-02: Data channel for VAD/state messages
  - TRANSPORT-03: Opus codec handled by aiortc
</success_criteria>

<output>
After completion, create `.planning/phases/08-webrtc-transport/08-04-SUMMARY.md`
</output>
