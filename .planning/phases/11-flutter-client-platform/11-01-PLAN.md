---
phase: 11-flutter-client-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/android/app/build.gradle
  - client/android/app/src/main/AndroidManifest.xml
  - client/ios/Runner/Info.plist
  - client/ios/Podfile
  - client/ios/Runner.xcodeproj/project.pbxproj
autonomous: false

must_haves:
  truths:
    - "Android APK builds successfully"
    - "iOS IPA builds successfully (or archive if no signing)"
    - "Android app installs and launches on device/emulator"
    - "iOS app installs and launches on device/simulator"
  artifacts:
    - path: "client/android/app/build.gradle"
      provides: "Android build configuration with release signing placeholder"
      contains: "applicationId = \"com.ergos.client\""
    - path: "client/android/app/src/main/AndroidManifest.xml"
      provides: "Android permissions and app metadata"
      contains: "android:label=\"Ergos\""
    - path: "client/ios/Runner/Info.plist"
      provides: "iOS app configuration with permissions"
      contains: "CFBundleDisplayName"
    - path: "client/ios/Podfile"
      provides: "iOS dependencies with correct deployment target"
      contains: "platform :ios"
  key_links:
    - from: "build.gradle"
      to: "AndroidManifest.xml"
      via: "applicationId matching package"
      pattern: "com\\.ergos\\.client"
    - from: "Podfile"
      to: "project.pbxproj"
      via: "IPHONEOS_DEPLOYMENT_TARGET"
      pattern: "12\\.0"
---

<objective>
Finalize Android and iOS platform configuration and verify builds.

Purpose: Ensure the Flutter client can be built and deployed to Android and iOS devices for real-world testing.
Output: Working APK for Android, archive/IPA for iOS, both verified to launch.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-flutter-client-ui/10-02-SUMMARY.md

@client/pubspec.yaml
@client/android/app/build.gradle
@client/android/app/src/main/AndroidManifest.xml
@client/ios/Runner/Info.plist
@client/ios/Podfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Android configuration</name>
  <files>client/android/app/build.gradle, client/android/app/src/main/AndroidManifest.xml</files>
  <action>
    1. Update AndroidManifest.xml:
       - Change android:label from "client" to "Ergos"
       - Verify all permissions are present: INTERNET, RECORD_AUDIO, MODIFY_AUDIO_SETTINGS, ACCESS_NETWORK_STATE

    2. Update build.gradle:
       - Verify applicationId is "com.ergos.client"
       - Verify minSdk is 23 (flutter_webrtc requirement)
       - Add comment about release signing placeholder

    Note: Keep debug signing for now - release signing requires user's keystore
  </action>
  <verify>flutter build apk --debug succeeds</verify>
  <done>Android debug APK builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Finalize iOS configuration</name>
  <files>client/ios/Runner/Info.plist, client/ios/Podfile, client/ios/Runner.xcodeproj/project.pbxproj</files>
  <action>
    1. Update Info.plist:
       - Change CFBundleDisplayName from "Client" to "Ergos"
       - Change CFBundleName from "client" to "Ergos"
       - Verify NSMicrophoneUsageDescription is present with appropriate message

    2. Verify Podfile:
       - platform :ios should be '12.0' (flutter_webrtc minimum)
       - use_frameworks! present (required for some plugins)

    3. Verify project.pbxproj:
       - IPHONEOS_DEPLOYMENT_TARGET should be 12.0
       - PRODUCT_BUNDLE_IDENTIFIER should match (com.ergos.client)

    Note: No code signing changes - that requires user's Apple Developer account
  </action>
  <verify>cd client && flutter build ios --no-codesign succeeds (or flutter build ios --simulator)</verify>
  <done>iOS builds without code signing errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Finalized Android and iOS platform configuration with proper app names and permissions</what-built>
  <how-to-verify>
    1. For Android (if you have a device/emulator):
       - Run: cd client && flutter run -d android
       - Verify: App launches with "Ergos" name
       - Verify: App icon appears in app drawer
       - Verify: Microphone permission prompt appears when connecting

    2. For iOS (if you have a Mac with Xcode):
       - Run: cd client && flutter run -d ios (simulator)
       - Verify: App launches with "Ergos" name
       - Verify: Microphone permission dialog shows correct message

    3. Alternatively, verify builds exist:
       - Android: ls -la client/build/app/outputs/flutter-apk/
       - iOS: ls -la client/build/ios/
  </how-to-verify>
  <resume-signal>Type "approved" if builds work, or describe any platform-specific issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter build apk --debug` succeeds
- [ ] `flutter build ios --no-codesign` succeeds (or --simulator)
- [ ] No lint errors in platform-specific files
- [ ] App name shows as "Ergos" in both platforms
</verification>

<success_criteria>

- Android APK builds successfully
- iOS archive builds successfully (without code signing)
- App names display correctly as "Ergos"
- Required permissions configured on both platforms
- Human verified at least one platform launches correctly
</success_criteria>

<output>
After completion, create `.planning/phases/11-flutter-client-platform/11-01-SUMMARY.md`
</output>
