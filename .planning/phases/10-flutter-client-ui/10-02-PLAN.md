---
phase: 10-flutter-client-ui
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - client/lib/main.dart
autonomous: true
must_haves:
  truths:
    - "App displays animated orb instead of text-based state display"
    - "Orb pulses when server is in LISTENING, PROCESSING, or SPEAKING state"
    - "Tapping orb during SPEAKING state sends barge_in message"
  artifacts:
    - path: "client/lib/main.dart"
      provides: "App with orb UI and barge-in functionality"
      min_lines: 150
  key_links:
    - from: "main.dart"
      to: "ergos_orb.dart"
      via: "ErgosOrb widget usage"
      pattern: "ErgosOrb"
    - from: "main.dart"
      to: "webrtc_service.dart"
      via: "barge_in message"
      pattern: "barge_in|sendDataChannelMessage"
---

<objective>
Wire ErgosOrb into main.dart, replacing text-based UI with animated orb and adding barge-in functionality.

Purpose: Complete the visual UI transformation with interactive barge-in capability.
Output: App displays animated orb that reflects server state and supports tap-to-interrupt.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-flutter-client-ui/10-RESEARCH.md
@.planning/phases/10-flutter-client-ui/10-01-SUMMARY.md

Data channel protocol for barge-in:
- Client sends: {"type": "barge_in", "timestamp": float}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main.dart with orb UI and barge-in</name>
  <files>client/lib/main.dart</files>
  <action>
    Update client/lib/main.dart to use ErgosOrb widget:

    1. Add import for widgets:
       ```dart
       import 'widgets/ergos_orb.dart';
       ```

    2. Add _sendBargeIn() method to _ErgosAppState:
       ```dart
       void _sendBargeIn() {
         if (_serverState == 'SPEAKING') {
           _webRTCService.sendDataChannelMessage({
             'type': 'barge_in',
             'timestamp': DateTime.now().millisecondsSinceEpoch / 1000,
           });
         }
       }
       ```

    3. Replace the body content in build() with new layout:
       - Keep Scaffold with AppBar
       - Replace Column with new layout:
         - SafeArea wrapping Column
         - Expanded widget containing Center with ErgosOrb
         - ErgosOrb: serverState: _serverState, onBargeIn: _sendBargeIn
         - Below orb: Small status section showing:
           - Connection state (small text)
           - Server state label (small text)
         - At bottom: Connect/Disconnect button (keep existing logic)

    4. Update theme to dark mode for better orb visibility:
       ```dart
       theme: ThemeData(
         colorScheme: ColorScheme.fromSeed(
           seedColor: Colors.blue,
           brightness: Brightness.dark,
         ),
         useMaterial3: true,
       ),
       ```

    5. Change background to dark color:
       - Scaffold backgroundColor: Color(0xFF1A1A2E)

    Layout structure:
    ```
    SafeArea
    └── Column
        ├── Expanded
        │   └── Center
        │       └── Column (mainAxisSize: min)
        │           ├── ErgosOrb (serverState, onBargeIn)
        │           └── SizedBox(height: 24)
        │           └── Text("Tap to interrupt" or "")
        ├── Padding (status info - small, subtle)
        │   └── Column
        │       ├── Text("Connection: ${state}")
        │       └── Text("Server: $_serverState")
        └── Padding (bottom button area)
            └── ElevatedButton (Connect/Disconnect)
    ```

    Show "Tap to interrupt" hint only when _serverState == 'SPEAKING'
  </action>
  <verify>cd client && flutter analyze lib/main.dart shows no errors</verify>
  <done>App displays orb with barge-in tap functionality</done>
</task>

<task type="auto">
  <name>Task 2: Final verification and build</name>
  <files>client/lib/main.dart</files>
  <action>
    Run final verification:
    1. flutter analyze - should show no errors
    2. flutter build web - should succeed (confirms Dart compiles)

    Verify key integrations:
    - ErgosOrb receives _serverState from _ErgosAppState
    - onBargeIn callback wired to _sendBargeIn
    - _sendBargeIn sends correct message format via WebRTC data channel
  </action>
  <verify>cd client && flutter analyze && flutter build web 2>&1 | tail -5</verify>
  <done>App builds successfully with orb UI</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd client && flutter analyze` shows no errors
- [ ] `cd client && flutter build web` succeeds
- [ ] ErgosOrb widget is used in main.dart
- [ ] Barge-in sends {"type": "barge_in", "timestamp": ...} via data channel
- [ ] Dark theme applied for better orb visibility
- [ ] "Tap to interrupt" hint shows only during SPEAKING state
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- App builds successfully
- Orb displays and animates based on server state
- Barge-in functionality wired correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-flutter-client-ui/10-02-SUMMARY.md`
</output>
