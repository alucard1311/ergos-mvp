---
phase: 12-integration-latency
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - tests/test_integration.py
  - README.md
autonomous: false

must_haves:
  truths:
    - "Server starts and accepts WebRTC connections"
    - "Voice-to-voice loop completes end-to-end"
    - "Latency metrics are logged during interaction"
  artifacts:
    - path: "tests/test_integration.py"
      provides: "Integration test script"
      min_lines: 30
    - path: "README.md"
      provides: "Usage instructions for running server and client"
      min_lines: 50
  key_links:
    - from: "README.md"
      to: "cli.py"
      via: "ergos start command"
      pattern: "ergos start"
---

<objective>
Create integration test and usage documentation.

Purpose: Verify end-to-end voice loop works and document how to run the system.
Output: Test script and README with usage instructions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-integration-latency/12-01-SUMMARY.md
@.planning/phases/12-integration-latency/12-02-SUMMARY.md

@src/ergos/cli.py
@src/ergos/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test script</name>
  <files>tests/test_integration.py</files>
  <action>
    Create `tests/test_integration.py`:

    1. Import pytest, asyncio, aiohttp

    2. Create test_server_starts():
       - Load default config
       - Check Server can be instantiated
       - Verify pipeline imports work

    3. Create test_signaling_endpoint():
       - Start aiohttp test server with signaling app
       - POST to /offer with mock SDP
       - Verify response contains SDP answer

    4. Create test_pipeline_wiring():
       - Create pipeline with create_pipeline()
       - Verify all components are instantiated
       - Verify callbacks are registered (check stats)

    Note: Full end-to-end test requires actual WebRTC which is tested manually.
    These tests verify the components wire together correctly.
  </action>
  <verify>python -m pytest tests/test_integration.py -v --collect-only succeeds</verify>
  <done>Integration tests verify component wiring</done>
</task>

<task type="auto">
  <name>Task 2: Create README with usage instructions</name>
  <files>README.md</files>
  <action>
    Create/update `README.md` with:

    1. Project title and description:
       - Ergos: Local-first voice assistant
       - Privacy-focused, all processing runs locally

    2. Quick Start section:
       ```bash
       # Install
       pip install -e .

       # Create config
       ergos setup

       # Download models (first run downloads automatically)
       # - Whisper: faster-whisper base.en
       # - LLM: Phi-3 Mini GGUF (user provides)
       # - TTS: Kokoro ONNX (auto-downloaded)

       # Start server
       ergos start

       # In another terminal, check status
       ergos status
       ```

    3. Client Connection section:
       - Flutter client connects to server IP:port
       - WebRTC signaling via /offer endpoint
       - Data channel for VAD events and state

    4. Configuration section:
       - config.yaml structure
       - Server host/port
       - Model paths (stt, llm, tts)
       - Persona file path

    5. Architecture section:
       - Brief pipeline description
       - Client sends audio via WebRTC
       - Server: STT → LLM → TTS
       - Response streams back via WebRTC

    6. Requirements section:
       - Python 3.11+
       - GPU recommended (CUDA for faster inference)
       - ~8GB RAM for full pipeline
  </action>
  <verify>test -f README.md && wc -l README.md shows at least 50 lines</verify>
  <done>README provides clear usage instructions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete voice assistant pipeline with integration tests and documentation</what-built>
  <how-to-verify>
    Note: Full end-to-end testing requires:
    - Server with GPU (for reasonable latency)
    - Flutter client on mobile device or emulator
    - Both on same network

    For verification without full hardware:
    1. Run: ergos setup (creates config.yaml)
    2. Run: ergos start (should show pipeline initialization logs)
    3. Check: Server logs show "Ergos server running on host:port"
    4. Check: Import test passes: python -c "from ergos import Pipeline"

    If you have full setup:
    1. Start server: ergos start
    2. Connect Flutter client to server IP
    3. Speak into phone
    4. Verify: Response plays back
    5. Check: Server logs show latency metrics

    Target latency (with GPU):
    - P50 < 500ms
    - P95 < 800ms
  </how-to-verify>
  <resume-signal>Type "approved" if pipeline works, or describe issues encountered</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Integration tests exist and collect successfully
- [ ] README has clear usage instructions
- [ ] Server starts without import errors
- [ ] Human verified at least server startup works
</verification>

<success_criteria>

- Integration test file exists
- README documents usage
- Server starts and initializes pipeline
- Human verified basic functionality
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-latency/12-03-SUMMARY.md`
</output>
