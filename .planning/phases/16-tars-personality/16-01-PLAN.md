---
phase: 16-tars-personality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ergos/persona/builder.py
  - src/ergos/persona/types.py
  - src/ergos/persona/loader.py
  - src/ergos/persona/tars.yaml
  - src/ergos/config.py
  - tests/unit/test_tars_personality.py
autonomous: true
requirements: [PERS-01]

must_haves:
  truths:
    - "Sarcasm level 0 produces a neutral TARS prompt (no humor, serious mission mode, still TARS identity)"
    - "Sarcasm level 100 produces a max-sarcasm prompt with deadpan humor and emotion hint guidance"
    - "Mid-range sarcasm (21-79) modulates humor frequency ('some' vs 'most' turns)"
    - "Voice command 'set sarcasm to N%' is parsed and returns clamped integer 0-100"
    - "PersonaConfig.sarcasm_level field defaults to 75, validates 0-100 via Pydantic"
    - "TARS is the default persona (DEFAULT_PERSONA.name == 'TARS', is_tars_persona == True)"
    - "Custom name substitution works (e.g. 'Jarvis' replaces 'TARS' in identity section)"
  artifacts:
    - path: "src/ergos/persona/builder.py"
      provides: "TARSPromptBuilder class, try_sarcasm_command(), get_time_context()"
      exports: ["TARSPromptBuilder", "try_sarcasm_command", "get_time_context"]
    - path: "src/ergos/persona/tars.yaml"
      provides: "Curated TARS persona YAML with is_tars_persona: true"
      contains: "is_tars_persona: true"
    - path: "src/ergos/persona/types.py"
      provides: "Persona dataclass with sarcasm_level and is_tars_persona fields"
      contains: "is_tars_persona"
    - path: "src/ergos/config.py"
      provides: "PersonaConfig with sarcasm_level: int = Field(default=75, ge=0, le=100)"
      contains: "sarcasm_level"
    - path: "tests/unit/test_tars_personality.py"
      provides: "Unit tests for prompt builder, sarcasm command, config, time context, default persona"
      min_lines: 100
  key_links:
    - from: "src/ergos/persona/builder.py"
      to: "src/ergos/persona/types.py"
      via: "TARS_NEUTRAL_SECTIONS / TARS_MAX_SARCASM_SECTIONS used by TARSPromptBuilder.build()"
      pattern: "TARSPromptBuilder"
    - from: "src/ergos/persona/loader.py"
      to: "src/ergos/persona/tars.yaml"
      via: "DEFAULT_PERSONA loads from tars.yaml as built-in TARS default"
      pattern: "DEFAULT_PERSONA"
    - from: "src/ergos/config.py"
      to: "src/ergos/persona/builder.py"
      via: "PersonaConfig.sarcasm_level consumed by pipeline to set TARSPromptBuilder sarcasm level"
      pattern: "sarcasm_level"
---

<objective>
Build the TARS personality infrastructure: prompt builder with section-based sarcasm blending, sarcasm voice command parser, time context helper, TARS persona YAML, and config/type extensions.

Purpose: Implements PERS-01 (configurable sarcasm 0-100%) and the TARS persona definition. This is the foundation that Phase 16's memory (plan 02) and pipeline wiring (plan 03) build on.
Output: TARSPromptBuilder class, try_sarcasm_command() parser, get_time_context() helper, tars.yaml persona file, extended PersonaConfig and Persona types, and full test suite.
</objective>

<execution_context>
@/home/vinay/.claude/get-shit-done/workflows/execute-plan.md
@/home/vinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tars-personality/16-CONTEXT.md
@.planning/phases/16-tars-personality/16-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/ergos/persona/types.py (EXTEND):
```python
@dataclass
class Persona:
    name: str
    description: str
    personality_traits: list[str] = field(default_factory=list)
    voice: str = "af_sarah"
    speaking_style: str = ""
    # ADD:
    sarcasm_level: int = 75
    is_tars_persona: bool = False
```

From src/ergos/config.py (EXTEND):
```python
class PersonaConfig(BaseModel):
    persona_file: Optional[str] = None
    name: str = "TARS"                    # CHANGED from "Ergos"
    system_prompt: str = "You are a helpful voice assistant."
    # ADD:
    sarcasm_level: int = Field(default=75, ge=0, le=100)
```

From src/ergos/persona/loader.py (EXTEND):
```python
DEFAULT_PERSONA = Persona(...)  # Must load from tars.yaml with is_tars_persona=True
def load_persona(path) -> Persona:  # Must support sarcasm_level and is_tars_persona fields
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TARSPromptBuilder, sarcasm command parser, time context helper, and test suite</name>
  <files>src/ergos/persona/builder.py, tests/unit/test_tars_personality.py</files>
  <behavior>
    - TestPromptBuilder::test_neutral_prompt_has_no_humor: sarcasm_level=0 -> prompt contains TARS identity but no humor/wit/funny keywords
    - TestPromptBuilder::test_max_sarcasm_prompt_has_humor: sarcasm_level=100 -> prompt contains humor/wit/deadpan AND emotion hint guidance (sighs/chuckles/ellipsis)
    - TestPromptBuilder::test_mid_sarcasm_prompt: sarcasm_level=50 -> prompt contains frequency modifier (most/some)
    - TestPromptBuilder::test_memories_injected: non-empty memories list -> prompt contains "What you know about the user:" block with each memory
    - TestPromptBuilder::test_empty_memories_omitted: empty memories -> no memory block in prompt
    - TestPromptBuilder::test_name_substitution: name="Jarvis" -> prompt contains "You are Jarvis"
    - TestPromptBuilder::test_time_context_injected: time_context string appears in prompt
    - TestSarcasmCommand::test_set_sarcasm_command_parsed: "set sarcasm to 80%" -> 80, "change sarcasm to 50 percent" -> 50
    - TestSarcasmCommand::test_non_sarcasm_input_returns_none: normal speech -> None
    - TestSarcasmCommand::test_sarcasm_clamped: 150% -> 100, -10% -> 0
    - TestTimeContext::test_time_context_periods: hour 3 -> "middle of the night", hour 9 -> "morning", hour 14 -> "afternoon", hour 19 -> "evening", hour 23 -> "late at night"
  </behavior>
  <action>
    Create src/ergos/persona/builder.py with:

    1. Two dict constants TARS_NEUTRAL_SECTIONS and TARS_MAX_SARCASM_SECTIONS, each with keys: identity, style, emotion, humor. Neutral has empty humor string and "Omit emotion hints" for emotion. Max sarcasm has deadpan humor and strategic emotion hint guidance (one hint per response max, use ellipsis for pauses).

    2. TARSPromptBuilder class with build(name, sarcasm_level, memories, time_context) -> str method. Three tiers: 0-20 uses neutral sections, 80-100 uses max sarcasm sections, 21-79 uses max identity/style/emotion with modulated humor frequency ("some" if < 50, "most" if >= 50). Clamp sarcasm_level to 0-100. Join non-empty sections with double newline. Inject time_context if non-empty. Inject memory block ("What you know about the user:\n- memory1\n- memory2") if memories non-empty.

    3. try_sarcasm_command(text) -> int | None using regex with lookbehind for negative values. Pattern matches "set/change/make/put ... sarcasm ... N". Clamp result to 0-100.

    4. get_time_context() -> str using datetime.now(). Map hour ranges to natural language periods. Return "It is the {period} on a {day}."

    Per CONTEXT.md locked decisions:
    - 0% is still TARS (competent, direct) but no humor
    - 100% is movie-accurate TARS (deadpan, not mean)
    - Section-based blending, not probability mixing
    - Style section must include "No markdown, bullet points, or numbered lists" and note that output is spoken aloud by TTS

    Create tests/unit/test_tars_personality.py with TestPromptBuilder, TestSarcasmCommand, and TestTimeContext classes covering all behaviors listed above. Use unittest.mock.patch for datetime mocking in time context tests.
  </action>
  <verify>
    <automated>/home/vinay/ergos-mvp/.venv/bin/python -m pytest tests/unit/test_tars_personality.py::TestPromptBuilder tests/unit/test_tars_personality.py::TestSarcasmCommand tests/unit/test_tars_personality.py::TestTimeContext -x -q</automated>
  </verify>
  <done>TARSPromptBuilder builds distinct prompts at 0%, 50%, 100% sarcasm levels. try_sarcasm_command parses voice commands and clamps values. get_time_context returns correct period strings. All 11 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend PersonaConfig, Persona types, loader, and create TARS persona YAML</name>
  <files>src/ergos/config.py, src/ergos/persona/types.py, src/ergos/persona/loader.py, src/ergos/persona/tars.yaml, tests/unit/test_tars_personality.py</files>
  <behavior>
    - TestPersonaConfig::test_sarcasm_level_field: PersonaConfig(sarcasm_level=80).sarcasm_level == 80
    - TestPersonaConfig::test_sarcasm_level_default: PersonaConfig().sarcasm_level == 75
    - TestPersonaConfig::test_sarcasm_level_rejects_over_100: PersonaConfig(sarcasm_level=101) raises ValidationError
    - TestPersonaConfig::test_sarcasm_level_rejects_negative: PersonaConfig(sarcasm_level=-1) raises ValidationError
    - TestTARSPersona::test_tars_is_default: DEFAULT_PERSONA.name == "TARS" and is_tars_persona == True
    - TestTARSPersona::test_custom_persona_file_overrides_default: load_persona(custom.yaml) loads non-TARS persona
  </behavior>
  <action>
    1. Extend src/ergos/config.py PersonaConfig:
       - Change name default from "Ergos" to "TARS" (per CONTEXT.md: TARS is v2 default)
       - Add sarcasm_level: int = Field(default=75, ge=0, le=100)

    2. Extend src/ergos/persona/types.py Persona dataclass:
       - Add sarcasm_level: int = 75 field
       - Add is_tars_persona: bool = False field
       - Keep existing system_prompt property unchanged (used by non-TARS personas)

    3. Create src/ergos/persona/tars.yaml with curated TARS persona:
       - name: "TARS", description: dry wit and deadpan, traits: precise/reliable/drily observational/never mean/understated
       - voice: "af_heart", speaking_style: deadpan and direct, sarcasm_level: 75, is_tars_persona: true

    4. Update src/ergos/persona/loader.py:
       - DEFAULT_PERSONA loads from built-in tars.yaml using importlib.resources or relative path
       - load_persona() handles sarcasm_level and is_tars_persona fields from YAML (with False defaults for backward compat)

    5. Add TestPersonaConfig and TestTARSPersona test classes to test_tars_personality.py.

    Per CONTEXT.md: persona_file set in config -> load that file. Only if null/unset does TARS default apply. Custom personas should not get is_tars_persona=True unless explicitly set.
  </action>
  <verify>
    <automated>/home/vinay/ergos-mvp/.venv/bin/python -m pytest tests/unit/test_tars_personality.py::TestPersonaConfig tests/unit/test_tars_personality.py::TestTARSPersona -x -q</automated>
  </verify>
  <done>PersonaConfig has sarcasm_level field with validation. Persona dataclass has sarcasm_level and is_tars_persona. DEFAULT_PERSONA is TARS with is_tars_persona=True. Custom persona files override default. All 6 config/persona tests pass.</done>
</task>

</tasks>

<verification>
/home/vinay/ergos-mvp/.venv/bin/python -m pytest tests/unit/test_tars_personality.py -v -q
</verification>

<success_criteria>
- TARSPromptBuilder produces distinct prompts at 0%, 50%, 100% sarcasm levels
- try_sarcasm_command parses "set sarcasm to N%" and clamps 0-100
- get_time_context returns correct period strings for all hour ranges
- PersonaConfig.sarcasm_level validates 0-100 with default 75
- DEFAULT_PERSONA is TARS with is_tars_persona=True
- Custom persona files are loaded without TARS override
- All 17 test_tars_personality.py tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-tars-personality/16-01-SUMMARY.md`
</output>
