---
phase: 16-tars-personality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ergos/memory/__init__.py
  - src/ergos/memory/types.py
  - src/ergos/memory/store.py
  - tests/unit/test_tars_memory.py
autonomous: true
requirements: [PERS-02, PERS-03]

must_haves:
  truths:
    - "MemoryStore.load() returns empty list when memory.json does not exist"
    - "MemoryStore.save() + load() round-trip preserves all MemoryEntry fields (content, category, timestamp, access_count)"
    - "MemoryStore.prune() drops lowest-scored entries when over MEMORY_MAX_STORED cap"
    - "MemoryStore.prune() respects access_count in scoring (higher access_count survives)"
    - "MemoryStore.get_budget() returns at most MEMORY_BUDGET entries sorted by recency"
    - "get_budget() increments access_count on returned entries"
    - "parse_extraction_result correctly categorizes preference, fact, moment lines"
    - "parse_extraction_result returns empty list for 'NOTHING' response"
    - "format_history_for_extraction returns None for fewer than 4 messages"
    - "Time context injection produces correct period strings for all hour ranges (PERS-02)"
    - "Built prompt contains memory entries when memories list is non-empty (PERS-02)"
  artifacts:
    - path: "src/ergos/memory/__init__.py"
      provides: "Package init exporting MemoryStore, MemoryEntry"
      exports: ["MemoryStore", "MemoryEntry"]
    - path: "src/ergos/memory/types.py"
      provides: "MemoryEntry dataclass with content, category, timestamp, access_count"
      contains: "class MemoryEntry"
    - path: "src/ergos/memory/store.py"
      provides: "MemoryStore class, parse_extraction_result, format_history_for_extraction, EXTRACTION_PROMPT"
      exports: ["MemoryStore", "parse_extraction_result", "format_history_for_extraction", "EXTRACTION_PROMPT"]
    - path: "tests/unit/test_tars_memory.py"
      provides: "Unit tests for MemoryStore CRUD, prune, budget, extraction parsing"
      min_lines: 100
  key_links:
    - from: "src/ergos/memory/store.py"
      to: "src/ergos/memory/types.py"
      via: "MemoryStore operates on list[MemoryEntry]"
      pattern: "MemoryEntry"
    - from: "src/ergos/memory/store.py"
      to: "~/.ergos/memory.json"
      via: "JSON persistence using dataclasses.asdict() and json.dumps/loads"
      pattern: "MEMORY_PATH"
---

<objective>
Build the cross-session memory subsystem: MemoryEntry type, MemoryStore with load/save/prune/budget operations, extraction prompt and parser, and history formatter.

Purpose: Implements PERS-03 (cross-session memory) and provides the memory injection infrastructure needed by PERS-02 (context-aware humor using past conversations). This module is independent of plan 01 (persona/builder) and can execute in parallel.
Output: Complete memory module (src/ergos/memory/) with types, store, extraction helpers, and full test suite.
</objective>

<execution_context>
@/home/vinay/.claude/get-shit-done/workflows/execute-plan.md
@/home/vinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tars-personality/16-CONTEXT.md
@.planning/phases/16-tars-personality/16-RESEARCH.md

<interfaces>
<!-- Existing patterns to follow -->

From src/ergos/plugins/kitchen/memory.py (PATTERN REFERENCE):
```python
# Kitchen plugin uses JSON at ~/.ergos/plugins/kitchen/memory.json
# with dataclass -> dict serialization via dataclasses.asdict()
# MemoryStore follows this exact pattern at ~/.ergos/memory.json
```

From src/ergos/llm/processor.py (CONSUMED):
```python
class Message:
    role: str      # "user" or "assistant"
    content: str
    timestamp: float

class LLMProcessor:
    @property
    def history(self) -> list[Message]: ...
```

From src/ergos/llm/generator.py (CONSUMED for extraction):
```python
class LLMGenerator:
    def generate(self, prompt: str, config: GenerationConfig) -> GenerationResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MemoryEntry type and MemoryStore with load/save/prune/budget operations</name>
  <files>src/ergos/memory/__init__.py, src/ergos/memory/types.py, src/ergos/memory/store.py, tests/unit/test_tars_memory.py</files>
  <behavior>
    - TestMemoryEntry::test_defaults: MemoryEntry("likes coffee", "preference") has auto timestamp and access_count=0
    - TestMemoryStore::test_load_empty: load() on non-existent file returns []
    - TestMemoryStore::test_roundtrip: save() + load() preserves content, category, timestamp, access_count
    - TestMemoryStore::test_prune: 110 entries pruned to 100, dropping 10 oldest (lowest timestamp)
    - TestMemoryStore::test_prune_respects_access_count: same-age entries with higher access_count survive
    - TestMemoryStore::test_budget: 20 entries -> get_budget returns 15, sorted most-recent-first
    - TestMemoryStore::test_budget_fewer_than_n: 5 entries -> returns all 5 when n=15
    - TestMemoryStore::test_budget_increments_access_count: returned entries have access_count incremented by 1
  </behavior>
  <action>
    1. Create src/ergos/memory/types.py:
       - MemoryEntry dataclass with: content (str), category (str), timestamp (float, default=time.time), access_count (int, default=0)

    2. Create src/ergos/memory/store.py:
       - Constants: MEMORY_PATH = Path.home() / ".ergos" / "memory.json", MEMORY_BUDGET = 15, MEMORY_MAX_STORED = 100
       - MemoryStore class with __init__(storage_path=None) defaulting to MEMORY_PATH
       - load() -> list[MemoryEntry]: returns [] if file missing, parses JSON dicts back to MemoryEntry
       - save(entries) -> None: creates parent dirs, writes JSON using dataclasses.asdict()
       - get_budget(entries, n=MEMORY_BUDGET) -> list[MemoryEntry]: sort by timestamp desc, return top n, increment access_count
       - prune(entries, max_size=MEMORY_MAX_STORED) -> list[MemoryEntry]: scoring formula 0.7*norm_timestamp + 0.3*norm_access_count, keep highest scored. Per locked decision: recency-weighted with frequency boost.

    3. Create src/ergos/memory/__init__.py exporting MemoryStore, MemoryEntry

    4. Create tests/unit/test_tars_memory.py with TestMemoryEntry and TestMemoryStore classes. Use tmp_path fixture for isolated file I/O.

    Per CONTEXT.md locked decisions:
    - Storage: JSON at ~/.ergos/memory.json (not under plugins/)
    - Pruning: oldest/least-used dropped when cap exceeded
    - Fixed budget injection pattern
  </action>
  <verify>
    <automated>/home/vinay/ergos-mvp/.venv/bin/python -m pytest tests/unit/test_tars_memory.py::TestMemoryEntry tests/unit/test_tars_memory.py::TestMemoryStore -x -q</automated>
  </verify>
  <done>MemoryEntry dataclass works with auto-timestamp. MemoryStore load/save round-trips correctly. Prune drops lowest-scored entries with recency-weighted formula. Budget returns top N by recency with access_count increment. All 8 store tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add extraction prompt, result parser, and history formatter</name>
  <files>src/ergos/memory/store.py, tests/unit/test_tars_memory.py</files>
  <behavior>
    - TestMemoryExtraction::test_parse_categories: "preference: likes dark coffee\nfact: works at a startup\nmoment: laughed at recursive joke" -> 3 MemoryEntry objects with correct categories
    - TestMemoryExtraction::test_parse_nothing: "NOTHING" -> empty list
    - TestMemoryExtraction::test_parse_mixed_case: "Preference: prefers tea" -> category lowercased to "preference"
    - TestMemoryExtraction::test_skip_short_history: 3 messages -> format_history returns None
    - TestMemoryExtraction::test_format_history: 6 messages -> formatted string with USER:/ASSISTANT: prefixes
  </behavior>
  <action>
    Add to src/ergos/memory/store.py:

    1. EXTRACTION_PROMPT constant: prompt template asking LLM to extract 0-5 memorable items categorized as preference/fact/moment. Includes "If nothing memorable occurred, respond with: NOTHING". Uses {history} placeholder.

    2. parse_extraction_result(text) -> list[MemoryEntry]: Parse "CATEGORY: sentence" lines. Lowercase category, validate against valid set {preference, fact, moment}. Return empty list for "NOTHING". Skip lines without ": " separator. Skip unknown categories.

    3. format_history_for_extraction(messages) -> Optional[str]: Takes list of Message-like objects with .role and .content. Returns None if len < 4 (skip extraction per Pitfall 6). Caps at last 20 messages. Formats as "USER: content\nASSISTANT: content".

    Add TestMemoryExtraction class to test_tars_memory.py. Import Message from ergos.llm.processor for history formatting tests.

    Per CONTEXT.md: LLM self-extraction at end of conversation, only extracted memories stored (no full logs), categories are preference/fact/moment.
  </action>
  <verify>
    <automated>/home/vinay/ergos-mvp/.venv/bin/python -m pytest tests/unit/test_tars_memory.py::TestMemoryExtraction -x -q</automated>
  </verify>
  <done>EXTRACTION_PROMPT template defined. parse_extraction_result handles all category cases and NOTHING response. format_history_for_extraction caps at 20 messages and skips short histories. All 5 extraction tests pass.</done>
</task>

</tasks>

<verification>
/home/vinay/ergos-mvp/.venv/bin/python -m pytest tests/unit/test_tars_memory.py -v -q
</verification>

<success_criteria>
- MemoryStore persists and loads entries via JSON round-trip
- Pruning drops lowest-scored entries using 0.7*recency + 0.3*access_count formula
- Budget returns top N most recent entries with access_count increment
- Extraction parser handles preference/fact/moment categories and NOTHING response
- History formatter skips short conversations (<4 messages) and caps at 20
- All 13 test_tars_memory.py tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-tars-personality/16-02-SUMMARY.md`
</output>
